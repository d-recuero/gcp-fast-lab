vpcs:
  spoke-dev:
    address_space:
      - 10.0.1.0/24
    subnets:
      - name: dev-app-subnet
        region: europe-west1
        ip_cidr_range: 10.0.1.0/24
    peering:
      peer_to_hub: true
      export_routes: true
      import_routes: true

    host_project: $project_ids:net-dev-0